from setuptools import setup, find_packages

setup(
    name='klabs.codeformer',
    version='1.0.0',
    description='Klabs Codeformer BasicSr',
    author='krokdev',
    author_email='krokdev@krokadile.com',
    packages=find_packages(),
    dependency_links=[
        
    ],
    install_requires=[
        # List of dependencies
    ],
)